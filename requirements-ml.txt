# Optional: only needed to (re)train the credit-intelligence model artifacts.
# The API runs without these -- see nuvix/credit/engine/registry.py for the
# transparent-fallback behaviour.
-r requirements.txt

numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1
joblib==1.4.2
lightgbm==4.5.0
