[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
addopts = -q --strict-markers --disable-warnings
testpaths = nuvix
markers =
    slow: tests that run a large number of simulations
